// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"moon/ent/assetcategory"
	"moon/ent/assets"
	"moon/ent/assettype"
	"moon/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"

	"moon/ent/internal"
)

// AssetCategoryQuery is the builder for querying AssetCategory entities.
type AssetCategoryQuery struct {
	config
	ctx                *QueryContext
	order              []assetcategory.OrderOption
	inters             []Interceptor
	predicates         []predicate.AssetCategory
	withAssetType      *AssetTypeQuery
	withAsset          *AssetsQuery
	modifiers          []func(*sql.Selector)
	loadTotal          []func(context.Context, []*AssetCategory) error
	withNamedAssetType map[string]*AssetTypeQuery
	withNamedAsset     map[string]*AssetsQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AssetCategoryQuery builder.
func (_q *AssetCategoryQuery) Where(ps ...predicate.AssetCategory) *AssetCategoryQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *AssetCategoryQuery) Limit(limit int) *AssetCategoryQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *AssetCategoryQuery) Offset(offset int) *AssetCategoryQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *AssetCategoryQuery) Unique(unique bool) *AssetCategoryQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *AssetCategoryQuery) Order(o ...assetcategory.OrderOption) *AssetCategoryQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryAssetType chains the current query on the "asset_type" edge.
func (_q *AssetCategoryQuery) QueryAssetType() *AssetTypeQuery {
	query := (&AssetTypeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(assetcategory.Table, assetcategory.FieldID, selector),
			sqlgraph.To(assettype.Table, assettype.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, assetcategory.AssetTypeTable, assetcategory.AssetTypeColumn),
		)
		schemaConfig := _q.schemaConfig
		step.To.Schema = schemaConfig.AssetType
		step.Edge.Schema = schemaConfig.AssetType
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAsset chains the current query on the "asset" edge.
func (_q *AssetCategoryQuery) QueryAsset() *AssetsQuery {
	query := (&AssetsClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(assetcategory.Table, assetcategory.FieldID, selector),
			sqlgraph.To(assets.Table, assets.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, assetcategory.AssetTable, assetcategory.AssetColumn),
		)
		schemaConfig := _q.schemaConfig
		step.To.Schema = schemaConfig.Assets
		step.Edge.Schema = schemaConfig.Assets
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AssetCategory entity from the query.
// Returns a *NotFoundError when no AssetCategory was found.
func (_q *AssetCategoryQuery) First(ctx context.Context) (*AssetCategory, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{assetcategory.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *AssetCategoryQuery) FirstX(ctx context.Context) *AssetCategory {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AssetCategory ID from the query.
// Returns a *NotFoundError when no AssetCategory ID was found.
func (_q *AssetCategoryQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{assetcategory.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *AssetCategoryQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AssetCategory entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AssetCategory entity is found.
// Returns a *NotFoundError when no AssetCategory entities are found.
func (_q *AssetCategoryQuery) Only(ctx context.Context) (*AssetCategory, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{assetcategory.Label}
	default:
		return nil, &NotSingularError{assetcategory.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *AssetCategoryQuery) OnlyX(ctx context.Context) *AssetCategory {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AssetCategory ID in the query.
// Returns a *NotSingularError when more than one AssetCategory ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *AssetCategoryQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{assetcategory.Label}
	default:
		err = &NotSingularError{assetcategory.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *AssetCategoryQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AssetCategories.
func (_q *AssetCategoryQuery) All(ctx context.Context) ([]*AssetCategory, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AssetCategory, *AssetCategoryQuery]()
	return withInterceptors[[]*AssetCategory](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *AssetCategoryQuery) AllX(ctx context.Context) []*AssetCategory {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AssetCategory IDs.
func (_q *AssetCategoryQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(assetcategory.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *AssetCategoryQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *AssetCategoryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*AssetCategoryQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *AssetCategoryQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *AssetCategoryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *AssetCategoryQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AssetCategoryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *AssetCategoryQuery) Clone() *AssetCategoryQuery {
	if _q == nil {
		return nil
	}
	return &AssetCategoryQuery{
		config:        _q.config,
		ctx:           _q.ctx.Clone(),
		order:         append([]assetcategory.OrderOption{}, _q.order...),
		inters:        append([]Interceptor{}, _q.inters...),
		predicates:    append([]predicate.AssetCategory{}, _q.predicates...),
		withAssetType: _q.withAssetType.Clone(),
		withAsset:     _q.withAsset.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithAssetType tells the query-builder to eager-load the nodes that are connected to
// the "asset_type" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AssetCategoryQuery) WithAssetType(opts ...func(*AssetTypeQuery)) *AssetCategoryQuery {
	query := (&AssetTypeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAssetType = query
	return _q
}

// WithAsset tells the query-builder to eager-load the nodes that are connected to
// the "asset" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AssetCategoryQuery) WithAsset(opts ...func(*AssetsQuery)) *AssetCategoryQuery {
	query := (&AssetsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAsset = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CategoryName string `json:"category_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AssetCategory.Query().
//		GroupBy(assetcategory.FieldCategoryName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *AssetCategoryQuery) GroupBy(field string, fields ...string) *AssetCategoryGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AssetCategoryGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = assetcategory.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CategoryName string `json:"category_name,omitempty"`
//	}
//
//	client.AssetCategory.Query().
//		Select(assetcategory.FieldCategoryName).
//		Scan(ctx, &v)
func (_q *AssetCategoryQuery) Select(fields ...string) *AssetCategorySelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &AssetCategorySelect{AssetCategoryQuery: _q}
	sbuild.label = assetcategory.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AssetCategorySelect configured with the given aggregations.
func (_q *AssetCategoryQuery) Aggregate(fns ...AggregateFunc) *AssetCategorySelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *AssetCategoryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !assetcategory.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *AssetCategoryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AssetCategory, error) {
	var (
		nodes       = []*AssetCategory{}
		_spec       = _q.querySpec()
		loadedTypes = [2]bool{
			_q.withAssetType != nil,
			_q.withAsset != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AssetCategory).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AssetCategory{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = _q.schemaConfig.AssetCategory
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withAssetType; query != nil {
		if err := _q.loadAssetType(ctx, query, nodes,
			func(n *AssetCategory) { n.Edges.AssetType = []*AssetType{} },
			func(n *AssetCategory, e *AssetType) { n.Edges.AssetType = append(n.Edges.AssetType, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAsset; query != nil {
		if err := _q.loadAsset(ctx, query, nodes,
			func(n *AssetCategory) { n.Edges.Asset = []*Assets{} },
			func(n *AssetCategory, e *Assets) { n.Edges.Asset = append(n.Edges.Asset, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedAssetType {
		if err := _q.loadAssetType(ctx, query, nodes,
			func(n *AssetCategory) { n.appendNamedAssetType(name) },
			func(n *AssetCategory, e *AssetType) { n.appendNamedAssetType(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedAsset {
		if err := _q.loadAsset(ctx, query, nodes,
			func(n *AssetCategory) { n.appendNamedAsset(name) },
			func(n *AssetCategory, e *Assets) { n.appendNamedAsset(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *AssetCategoryQuery) loadAssetType(ctx context.Context, query *AssetTypeQuery, nodes []*AssetCategory, init func(*AssetCategory), assign func(*AssetCategory, *AssetType)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*AssetCategory)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(assettype.FieldAssetCategoryID)
	}
	query.Where(predicate.AssetType(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(assetcategory.AssetTypeColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AssetCategoryID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "asset_category_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *AssetCategoryQuery) loadAsset(ctx context.Context, query *AssetsQuery, nodes []*AssetCategory, init func(*AssetCategory), assign func(*AssetCategory, *Assets)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*AssetCategory)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(assets.FieldAssetCategoryID)
	}
	query.Where(predicate.Assets(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(assetcategory.AssetColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.AssetCategoryID
		if fk == nil {
			return fmt.Errorf(`foreign-key "asset_category_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "asset_category_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *AssetCategoryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Schema = _q.schemaConfig.AssetCategory
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *AssetCategoryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(assetcategory.Table, assetcategory.Columns, sqlgraph.NewFieldSpec(assetcategory.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, assetcategory.FieldID)
		for i := range fields {
			if fields[i] != assetcategory.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *AssetCategoryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(assetcategory.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = assetcategory.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	t1.Schema(_q.schemaConfig.AssetCategory)
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	selector.WithContext(ctx)
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedAssetType tells the query-builder to eager-load the nodes that are connected to the "asset_type"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *AssetCategoryQuery) WithNamedAssetType(name string, opts ...func(*AssetTypeQuery)) *AssetCategoryQuery {
	query := (&AssetTypeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedAssetType == nil {
		_q.withNamedAssetType = make(map[string]*AssetTypeQuery)
	}
	_q.withNamedAssetType[name] = query
	return _q
}

// WithNamedAsset tells the query-builder to eager-load the nodes that are connected to the "asset"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *AssetCategoryQuery) WithNamedAsset(name string, opts ...func(*AssetsQuery)) *AssetCategoryQuery {
	query := (&AssetsClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedAsset == nil {
		_q.withNamedAsset = make(map[string]*AssetsQuery)
	}
	_q.withNamedAsset[name] = query
	return _q
}

// AssetCategoryGroupBy is the group-by builder for AssetCategory entities.
type AssetCategoryGroupBy struct {
	selector
	build *AssetCategoryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *AssetCategoryGroupBy) Aggregate(fns ...AggregateFunc) *AssetCategoryGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *AssetCategoryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AssetCategoryQuery, *AssetCategoryGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *AssetCategoryGroupBy) sqlScan(ctx context.Context, root *AssetCategoryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AssetCategorySelect is the builder for selecting fields of AssetCategory entities.
type AssetCategorySelect struct {
	*AssetCategoryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *AssetCategorySelect) Aggregate(fns ...AggregateFunc) *AssetCategorySelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *AssetCategorySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AssetCategoryQuery, *AssetCategorySelect](ctx, _s.AssetCategoryQuery, _s, _s.inters, v)
}

func (_s *AssetCategorySelect) sqlScan(ctx context.Context, root *AssetCategoryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
