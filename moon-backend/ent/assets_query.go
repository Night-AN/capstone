// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"moon/ent/assetcategory"
	"moon/ent/assets"
	"moon/ent/assettype"
	"moon/ent/organization"
	"moon/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"

	"moon/ent/internal"
)

// AssetsQuery is the builder for querying Assets entities.
type AssetsQuery struct {
	config
	ctx               *QueryContext
	order             []assets.OrderOption
	inters            []Interceptor
	predicates        []predicate.Assets
	withAssetCategory *AssetCategoryQuery
	withAssetType     *AssetTypeQuery
	withOrganization  *OrganizationQuery
	modifiers         []func(*sql.Selector)
	loadTotal         []func(context.Context, []*Assets) error
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AssetsQuery builder.
func (_q *AssetsQuery) Where(ps ...predicate.Assets) *AssetsQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *AssetsQuery) Limit(limit int) *AssetsQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *AssetsQuery) Offset(offset int) *AssetsQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *AssetsQuery) Unique(unique bool) *AssetsQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *AssetsQuery) Order(o ...assets.OrderOption) *AssetsQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryAssetCategory chains the current query on the "asset_category" edge.
func (_q *AssetsQuery) QueryAssetCategory() *AssetCategoryQuery {
	query := (&AssetCategoryClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(assets.Table, assets.FieldID, selector),
			sqlgraph.To(assetcategory.Table, assetcategory.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, assets.AssetCategoryTable, assets.AssetCategoryColumn),
		)
		schemaConfig := _q.schemaConfig
		step.To.Schema = schemaConfig.AssetCategory
		step.Edge.Schema = schemaConfig.Assets
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAssetType chains the current query on the "asset_type" edge.
func (_q *AssetsQuery) QueryAssetType() *AssetTypeQuery {
	query := (&AssetTypeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(assets.Table, assets.FieldID, selector),
			sqlgraph.To(assettype.Table, assettype.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, assets.AssetTypeTable, assets.AssetTypeColumn),
		)
		schemaConfig := _q.schemaConfig
		step.To.Schema = schemaConfig.AssetType
		step.Edge.Schema = schemaConfig.Assets
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOrganization chains the current query on the "organization" edge.
func (_q *AssetsQuery) QueryOrganization() *OrganizationQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(assets.Table, assets.FieldID, selector),
			sqlgraph.To(organization.Table, organization.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, assets.OrganizationTable, assets.OrganizationColumn),
		)
		schemaConfig := _q.schemaConfig
		step.To.Schema = schemaConfig.Organization
		step.Edge.Schema = schemaConfig.Assets
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Assets entity from the query.
// Returns a *NotFoundError when no Assets was found.
func (_q *AssetsQuery) First(ctx context.Context) (*Assets, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{assets.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *AssetsQuery) FirstX(ctx context.Context) *Assets {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Assets ID from the query.
// Returns a *NotFoundError when no Assets ID was found.
func (_q *AssetsQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{assets.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *AssetsQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Assets entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Assets entity is found.
// Returns a *NotFoundError when no Assets entities are found.
func (_q *AssetsQuery) Only(ctx context.Context) (*Assets, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{assets.Label}
	default:
		return nil, &NotSingularError{assets.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *AssetsQuery) OnlyX(ctx context.Context) *Assets {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Assets ID in the query.
// Returns a *NotSingularError when more than one Assets ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *AssetsQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{assets.Label}
	default:
		err = &NotSingularError{assets.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *AssetsQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AssetsSlice.
func (_q *AssetsQuery) All(ctx context.Context) ([]*Assets, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Assets, *AssetsQuery]()
	return withInterceptors[[]*Assets](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *AssetsQuery) AllX(ctx context.Context) []*Assets {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Assets IDs.
func (_q *AssetsQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(assets.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *AssetsQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *AssetsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*AssetsQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *AssetsQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *AssetsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *AssetsQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AssetsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *AssetsQuery) Clone() *AssetsQuery {
	if _q == nil {
		return nil
	}
	return &AssetsQuery{
		config:            _q.config,
		ctx:               _q.ctx.Clone(),
		order:             append([]assets.OrderOption{}, _q.order...),
		inters:            append([]Interceptor{}, _q.inters...),
		predicates:        append([]predicate.Assets{}, _q.predicates...),
		withAssetCategory: _q.withAssetCategory.Clone(),
		withAssetType:     _q.withAssetType.Clone(),
		withOrganization:  _q.withOrganization.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithAssetCategory tells the query-builder to eager-load the nodes that are connected to
// the "asset_category" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AssetsQuery) WithAssetCategory(opts ...func(*AssetCategoryQuery)) *AssetsQuery {
	query := (&AssetCategoryClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAssetCategory = query
	return _q
}

// WithAssetType tells the query-builder to eager-load the nodes that are connected to
// the "asset_type" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AssetsQuery) WithAssetType(opts ...func(*AssetTypeQuery)) *AssetsQuery {
	query := (&AssetTypeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAssetType = query
	return _q
}

// WithOrganization tells the query-builder to eager-load the nodes that are connected to
// the "organization" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AssetsQuery) WithOrganization(opts ...func(*OrganizationQuery)) *AssetsQuery {
	query := (&OrganizationClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOrganization = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		AssetName string `json:"asset_name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Assets.Query().
//		GroupBy(assets.FieldAssetName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *AssetsQuery) GroupBy(field string, fields ...string) *AssetsGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AssetsGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = assets.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		AssetName string `json:"asset_name,omitempty"`
//	}
//
//	client.Assets.Query().
//		Select(assets.FieldAssetName).
//		Scan(ctx, &v)
func (_q *AssetsQuery) Select(fields ...string) *AssetsSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &AssetsSelect{AssetsQuery: _q}
	sbuild.label = assets.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AssetsSelect configured with the given aggregations.
func (_q *AssetsQuery) Aggregate(fns ...AggregateFunc) *AssetsSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *AssetsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !assets.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *AssetsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Assets, error) {
	var (
		nodes       = []*Assets{}
		_spec       = _q.querySpec()
		loadedTypes = [3]bool{
			_q.withAssetCategory != nil,
			_q.withAssetType != nil,
			_q.withOrganization != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Assets).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Assets{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	_spec.Node.Schema = _q.schemaConfig.Assets
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withAssetCategory; query != nil {
		if err := _q.loadAssetCategory(ctx, query, nodes, nil,
			func(n *Assets, e *AssetCategory) { n.Edges.AssetCategory = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAssetType; query != nil {
		if err := _q.loadAssetType(ctx, query, nodes, nil,
			func(n *Assets, e *AssetType) { n.Edges.AssetType = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOrganization; query != nil {
		if err := _q.loadOrganization(ctx, query, nodes, nil,
			func(n *Assets, e *Organization) { n.Edges.Organization = e }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *AssetsQuery) loadAssetCategory(ctx context.Context, query *AssetCategoryQuery, nodes []*Assets, init func(*Assets), assign func(*Assets, *AssetCategory)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Assets)
	for i := range nodes {
		if nodes[i].AssetCategoryID == nil {
			continue
		}
		fk := *nodes[i].AssetCategoryID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(assetcategory.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "asset_category_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AssetsQuery) loadAssetType(ctx context.Context, query *AssetTypeQuery, nodes []*Assets, init func(*Assets), assign func(*Assets, *AssetType)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Assets)
	for i := range nodes {
		if nodes[i].AssetTypeID == nil {
			continue
		}
		fk := *nodes[i].AssetTypeID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(assettype.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "asset_type_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *AssetsQuery) loadOrganization(ctx context.Context, query *OrganizationQuery, nodes []*Assets, init func(*Assets), assign func(*Assets, *Organization)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Assets)
	for i := range nodes {
		if nodes[i].OrganizationID == nil {
			continue
		}
		fk := *nodes[i].OrganizationID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(organization.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "organization_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *AssetsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Schema = _q.schemaConfig.Assets
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *AssetsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(assets.Table, assets.Columns, sqlgraph.NewFieldSpec(assets.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, assets.FieldID)
		for i := range fields {
			if fields[i] != assets.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withAssetCategory != nil {
			_spec.Node.AddColumnOnce(assets.FieldAssetCategoryID)
		}
		if _q.withAssetType != nil {
			_spec.Node.AddColumnOnce(assets.FieldAssetTypeID)
		}
		if _q.withOrganization != nil {
			_spec.Node.AddColumnOnce(assets.FieldOrganizationID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *AssetsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(assets.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = assets.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	t1.Schema(_q.schemaConfig.Assets)
	ctx = internal.NewSchemaConfigContext(ctx, _q.schemaConfig)
	selector.WithContext(ctx)
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AssetsGroupBy is the group-by builder for Assets entities.
type AssetsGroupBy struct {
	selector
	build *AssetsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *AssetsGroupBy) Aggregate(fns ...AggregateFunc) *AssetsGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *AssetsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AssetsQuery, *AssetsGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *AssetsGroupBy) sqlScan(ctx context.Context, root *AssetsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AssetsSelect is the builder for selecting fields of Assets entities.
type AssetsSelect struct {
	*AssetsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *AssetsSelect) Aggregate(fns ...AggregateFunc) *AssetsSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *AssetsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AssetsQuery, *AssetsSelect](ctx, _s.AssetsQuery, _s, _s.inters, v)
}

func (_s *AssetsSelect) sqlScan(ctx context.Context, root *AssetsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
