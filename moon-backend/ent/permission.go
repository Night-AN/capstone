// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"moon/ent/permission"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// Permission is the model entity for the Permission schema.
type Permission struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// PermissionName holds the value of the "permission_name" field.
	PermissionName string `json:"permission_name,omitempty"`
	// PermissionCode holds the value of the "permission_code" field.
	PermissionCode string `json:"permission_code,omitempty"`
	// PermissionDescription holds the value of the "permission_description" field.
	PermissionDescription string `json:"permission_description,omitempty"`
	// PermissionFlag holds the value of the "permission_flag" field.
	PermissionFlag string `json:"permission_flag,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PermissionQuery when eager-loading is set.
	Edges        PermissionEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PermissionEdges holds the relations/edges for other nodes in the graph.
type PermissionEdges struct {
	// Role holds the value of the role edge.
	Role []*Role `json:"role,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
	// totalCount holds the count of the edges above.
	totalCount [1]map[string]int

	namedRole map[string][]*Role
}

// RoleOrErr returns the Role value or an error if the edge
// was not loaded in eager-loading.
func (e PermissionEdges) RoleOrErr() ([]*Role, error) {
	if e.loadedTypes[0] {
		return e.Role, nil
	}
	return nil, &NotLoadedError{edge: "role"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Permission) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case permission.FieldPermissionName, permission.FieldPermissionCode, permission.FieldPermissionDescription, permission.FieldPermissionFlag:
			values[i] = new(sql.NullString)
		case permission.FieldCreatedAt, permission.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case permission.FieldID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Permission fields.
func (_m *Permission) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case permission.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case permission.FieldPermissionName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field permission_name", values[i])
			} else if value.Valid {
				_m.PermissionName = value.String
			}
		case permission.FieldPermissionCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field permission_code", values[i])
			} else if value.Valid {
				_m.PermissionCode = value.String
			}
		case permission.FieldPermissionDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field permission_description", values[i])
			} else if value.Valid {
				_m.PermissionDescription = value.String
			}
		case permission.FieldPermissionFlag:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field permission_flag", values[i])
			} else if value.Valid {
				_m.PermissionFlag = value.String
			}
		case permission.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case permission.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Permission.
// This includes values selected through modifiers, order, etc.
func (_m *Permission) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryRole queries the "role" edge of the Permission entity.
func (_m *Permission) QueryRole() *RoleQuery {
	return NewPermissionClient(_m.config).QueryRole(_m)
}

// Update returns a builder for updating this Permission.
// Note that you need to call Permission.Unwrap() before calling this method if this Permission
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Permission) Update() *PermissionUpdateOne {
	return NewPermissionClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Permission entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Permission) Unwrap() *Permission {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Permission is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Permission) String() string {
	var builder strings.Builder
	builder.WriteString("Permission(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("permission_name=")
	builder.WriteString(_m.PermissionName)
	builder.WriteString(", ")
	builder.WriteString("permission_code=")
	builder.WriteString(_m.PermissionCode)
	builder.WriteString(", ")
	builder.WriteString("permission_description=")
	builder.WriteString(_m.PermissionDescription)
	builder.WriteString(", ")
	builder.WriteString("permission_flag=")
	builder.WriteString(_m.PermissionFlag)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// NamedRole returns the Role named value or an error if the edge was not
// loaded in eager-loading with this name.
func (_m *Permission) NamedRole(name string) ([]*Role, error) {
	if _m.Edges.namedRole == nil {
		return nil, &NotLoadedError{edge: name}
	}
	nodes, ok := _m.Edges.namedRole[name]
	if !ok {
		return nil, &NotLoadedError{edge: name}
	}
	return nodes, nil
}

func (_m *Permission) appendNamedRole(name string, edges ...*Role) {
	if _m.Edges.namedRole == nil {
		_m.Edges.namedRole = make(map[string][]*Role)
	}
	if len(edges) == 0 {
		_m.Edges.namedRole[name] = []*Role{}
	} else {
		_m.Edges.namedRole[name] = append(_m.Edges.namedRole[name], edges...)
	}
}

// Permissions is a parsable slice of Permission.
type Permissions []*Permission
